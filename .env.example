# Copy this file to .env in the repo root (same folder as package.json):
#   cp .env.example .env
#
# Local: pnpm --filter @workspace/api-server dev
# Vercel: Project → Settings → Environment Variables (same names, no .env file)

OPENAI_API_KEY=sk-your-key-here
SITE_PASSWORD=676767
